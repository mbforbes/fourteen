# Files in home directory by default that we don't care about.
.ICEauthority
.Xauthority
.bash_history
.cache/
.config/
.dbus/
.dmrc
.gconf/
.gnome2/
.gtk-bookmarks
.local/
.mission-control/
.mozilla/
.nv/
.profile
.pulse-cookie
.pulse/
.xsession-errors
examples.desktop

# Files I eventually added to home directory we don't want
.compiz-1/
.fontconfig
.gazebo/
.gstreamer-0.10/
.lesshst
.matplotlib/
.pip/
.pki
.ros/
.rviz/
.ssh/
.xsession-errors.old
Downloads/
rqt_rviz_ogre.log

# This is where we'll install other software. Want them to
# be in separate git repos.
repos/

# Extra stuff we don't care about in general
*~
*.pyc

# ROS Catkin stuff
# http://answers.ros.org/question/10182/how-to-correctly-use-git-with-ros/
build/
# No pages online receommend ignore devel/, but maybe is good to ignore...
devel/
bin/
lib/
msg_gen/
srv_gen/

# ROS Rosbuild stuff
# Want to ignore the repos within (e.g. pr2_pbd)
rosbuild_ws/pr2_pbd/